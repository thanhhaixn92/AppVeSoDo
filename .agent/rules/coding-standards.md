# Coding Standards

- Prefer small, scoped changes.
- Avoid broad rewrites.
- TypeScript: avoid `any` unless justified.
- Keep policy in domain helpers, not duplicated in UI.
- Do not introduce dead APIs.
- Do not hard-code secrets or environment-specific URLs.
- Use clear names.
- Comment why, not what.
