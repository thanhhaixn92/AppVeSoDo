# R7-M1B-2 Prompt — Extract useCandidateWorkflow

You are working on SO DO APP / Academic Figure Creator.

Use `.agent/tasks/R7-M1B-2.yml` as the single source of truth for this task. Do not expand scope beyond that file.

## Current checkpoint

- M1A: AUDIT_PASS
- M1B-1: AUDIT_PASS
- M1B-2: NOT_STARTED / READY_TO_PROMPT
- latest verified commit: 3960ca5
- governance: Harness v1.1.1 COMPLETE

## Product context

SO DO APP / Academic Figure Creator is not only a long-document analysis assistant.

Candidate generation is one stage in the product pipeline. The full product pipeline is:

```text
input data/document
→ preserve raw input
→ generate visual candidates
→ select candidate
→ convert to renderable figure model
→ render with academic/professional templates
→ edit through properties panel or AI chat
→ preview
→ save
→ export
```

This product goal clarification does not expand R7-M1B-2 scope.

For this task, do not implement template engine, candidate-to-renderable adapter, properties panel, AI chat editing, persistence, or export changes.

## Goal

Extract candidate workflow state and handlers from `src/App.tsx` into `src/hooks/useCandidateWorkflow.ts`.

The result should make `App.tsx` more of an orchestration shell while preserving all M1A candidate policy behavior.

## Required context

Read in this order:

1. `AGENTS.md`
2. `.agent/context/project-state.yml`
3. `.agent/tasks/R7-M1B-2.yml`
4. `.agent/context/codebase-map.yml`
5. `.agent/context/module-index.yml`
6. `.agent/context/issue-memory.yml`
7. `.agent/context/root-cause-library.yml`
8. `.agent/context/file-ownership.yml`
9. `.agent/context/milestone-acceptance.yml`
10. `.agent/project/protected-areas.md`
11. `.agent/project/commands.md`
12. Task-specific source files listed in `.agent/tasks/R7-M1B-2.yml`

## Hard boundaries

Do not change:

- export DOM contract
- `data-export-target`
- renderer DOM wrappers
- SVG/PDF/PNG export behavior
- Firebase
- auth/user/permission
- real AI/Gemini runtime
- server boundary
- package dependencies
- M1C/M1D/M1E/M2/M3/M4 behavior
- template rendering engine
- properties panel
- AI chat editing
- persistence changes

Do not duplicate candidate policy outside `src/analysis/candidateModel.ts`.

## Required workflow

Follow:

```text
IMPLEMENT
→ SELF-CHECK
→ AUDIT-ONLY
→ CORRECTIVE if FAIL
→ RE-AUDIT
→ HUMAN CHECKPOINT
```

Audit-only must not edit, format, commit, or create PR.

## Implementation rules

- Make the smallest safe extraction.
- Create or update `src/hooks/useCandidateWorkflow.ts`.
- Keep candidate usability/preview/apply decisions routed through candidateModel helpers.
- Keep App.tsx as orchestration wiring only for this concern.
- Do not refactor analysis engine, canvas manager, export, persistence, Firebase, or real AI.
- Do not build the template rendering engine, figure properties panel, AI chat editing workflow, persistence, or export hardening in this task.

## Required commands

Run or honestly report inability to run:

```bash
npm test -- candidateModel
npm run lint
npm run build
npm test
npx playwright install --with-deps
npm run test:e2e -- --project=chromium
```

E2E classification must use `.agent/core/e2e-classification.md`.

Never report E2E PASS unless Chromium E2E actually runs and passes.

## Stop conditions

Stop and report if:

- candidate policy must be changed;
- export/renderer DOM changes appear necessary;
- Firebase/auth/server/dependency changes appear necessary;
- product or architecture decision is required;
- tests fail after 3 repair loops;
- root cause is unclear;
- E2E is blocked by environment.

## Final report

Use `.agent/core/universal-report-format.md`.

The report must explicitly list:

- changed files;
- commands run;
- test results;
- E2E classification;
- protected areas not changed;
- remaining risks;
- whether audit-only returned AUDIT_PASS or AUDIT_FAIL.
