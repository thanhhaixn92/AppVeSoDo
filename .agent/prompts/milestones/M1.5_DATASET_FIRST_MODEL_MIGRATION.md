# M1.5 Dataset-First Model Migration

## Instructions for Agent
1. **Read Context.** You MUST read `AGENTS.md` and `.agent/tasks/M1.5_DATASET_FIRST_MODEL_MIGRATION.yml` first before taking any action.
2. **Restate Constraints.** Before making any code edits, you MUST explicitly restate the allowed scope, forbidden scope, acceptance criteria, and planned files in your thought or output to confirm your understanding.
3. **Narrow Slice Only.** Implement only a very narrow slice (e.g., introducing type aliases or metadata fields) without changing runtime behavior.
4. **No Scope Expansion.** Do not attempt to migrate the entire application at once. Avoid changes to `App.tsx`, export DOM, renderer wrappers, Firebase, or real AI runtime unless explicitly authorized in your narrow slice plan.
5. **Honest Test Reporting.** Run `npm run build`, `npm test`, and relevant E2E tests (like `npm run test:e2e -- missing-payload-guard.spec.ts`). Report the results honestly. If E2E fails, debug and fix it, or report `BLOCKED_BY_ENVIRONMENT` if it cannot run.
6. **Final Report.** Once your slice is implemented and verified, generate a comprehensive report using the universal report format to document your changes.
