# React TypeScript Skill

Use for React/TypeScript implementation.

Guidelines:
- Functional components and hooks.
- Keep state ownership clear.
- Avoid duplicating domain policy in components.
- Add tests for behavior, not only helpers.
- Do not rewrite large components unless scoped.
