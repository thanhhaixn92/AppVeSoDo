# Protected Areas

Do not change without explicit scope:

- export DOM contract;
- renderer wrappers;
- `data-export-target`;
- SVG/PDF/PNG export behavior;
- Firebase;
- Auth/user/permission;
- real AI/Gemini runtime;
- server boundary;
- package dependencies;
- candidate policy after M1A;
- document workflow after M1B-1 unless explicitly scoped.
